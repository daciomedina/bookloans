{% extends 'base.html.twig' %}

{% block title %}BooksGenreDef{% endblock %}

{% block body %}
    <h1>BooksGenreDef</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ books_genre_def.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ books_genre_def.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('books_genre_def_index') }}">back to list</a>

    <a href="{{ path('books_genre_def_edit', {'id': books_genre_def.id}) }}">edit</a>

    {{ include('books_genre_def/_delete_form.html.twig') }}
{% endblock %}
