<form method="post" action="{{ path('books_genre_def_delete', {'id': books_genre_def.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ books_genre_def.id) }}">
    <button class="btn">Delete</button>
</form>
