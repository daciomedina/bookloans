<form method="post" action="{{ path('books_loans_delete', {'id': books_loan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ books_loan.id) }}">
    <button class="btn">Delete</button>
</form>
